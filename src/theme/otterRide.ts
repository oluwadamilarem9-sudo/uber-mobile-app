/** OtterRide visual tokens (aligned with `template/` mockups). */
export const otterColors = {
  primary: '#FFD000',
  ink: '#1A1A1A',
  surface: '#F5F5F5',
  surfaceMuted: '#EDEDED',
  white: '#FFFFFF',
} as const;
