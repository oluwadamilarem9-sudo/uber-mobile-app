/**
 * Fallback + TypeScript entry. Metro prefers `ExploreMapScreen.web.tsx` (web) and
 * `ExploreMapScreen.native.tsx` (iOS/Android), so this file is not bundled for web.
 */
export { ExploreMapScreen } from './ExploreMapScreen.native';
